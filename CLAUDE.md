# task-board

## プロジェクト概要

タスク管理ボードアプリケーション。

## Git 運用ルール

### 基本方針

- コードを変更するたびに、変更内容をコミットして GitHub にプッシュすること。
- コミットは小さく・意味のある単位で行う。「動いたからまとめてコミット」はしない。
- `main` ブランチへの直接プッシュは原則禁止。作業は必ずフィーチャーブランチで行う。

### ブランチ命名規則

```
feature/<機能名>    # 新機能
fix/<バグ概要>      # バグ修正
refactor/<対象>     # リファクタリング
docs/<対象>         # ドキュメント変更のみ
```

### コミットメッセージ規則

```
<type>: <変更内容の要約（日本語可）>
```

`type` の種類:
- `feat` — 新機能
- `fix` — バグ修正
- `refactor` — 動作を変えないコード変更
- `style` — フォーマット・命名など
- `test` — テストの追加・修正
- `docs` — ドキュメントのみの変更
- `chore` — ビルド設定・依存関係など

例:
```
feat: タスクの優先度フィルター機能を追加
fix: 期限切れタスクが削除できないバグを修正
```

### コード変更後のワークフロー

コードを変更するたびに以下の手順を実行すること:

```bash
git add <変更ファイル>
git commit -m "<type>: <変更内容>"
git push origin <ブランチ名>
```

Claude Code がコード変更を行った場合も同様に、変更後すぐにプッシュまで完了させること。

### プルリクエスト

- フィーチャーブランチの作業完了後は GitHub で PR を作成する。
- PR タイトルはコミットメッセージと同じ形式に従う。
- レビューなしで `main` にマージしない。

## 開発メモ

- このファイル (CLAUDE.md) はプロジェクトの規約・ルールの唯一の情報源として管理する。
- ルールを変更する場合はこのファイルを更新し、変更をコミット・プッシュすること。
